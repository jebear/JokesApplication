<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- Button -->
    <style name="CustomStyleButtonCurveOrange" parent="@android:style/Widget.Button">
        <item name="android:textSize">14sp</item>
        <item name="android:background">@drawable/border_rectangle_curve_orange</item>
        <item name="android:padding">3dip</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:width">200dp</item>
    </style>

    <style name="CustomStyleButtonCurveBlue" parent="@android:style/Widget.Button">
        <item name="android:textSize">12sp</item>
        <item name="android:background">@drawable/border_rectangle_curve_blue</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Radio Button -->
    <style name="RadioButtonStyle" parent="@android:style/Widget.CompoundButton">
        <item name="android:button">@drawable/radiobutton_selector</item>
        <item name="android:textColor">@color/grey</item>
        <item name="android:padding">5dip</item>
    </style>

    <!-- TextView -->
    <style name="CustomStyleTextName" parent="@android:style/TextAppearance">
        <item name="android:textSize">14sp</item>
        <item name="android:background">@drawable/border_rectangle_curve_name</item>
        <item name="android:padding">5dip</item>
        <item name="android:textColor">@color/grey</item>
        <item name="android:width">200dp</item>
        <item name="android:textColorHint">@color/grey</item>
    </style>

    <style name="CustomStyleText" parent="@android:style/TextAppearance">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/grey</item>
    </style>

    <!-- Dialog -->
    <style name="myDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@drawable/dialog_border_curve_white</item>
    </style>

</resources>
